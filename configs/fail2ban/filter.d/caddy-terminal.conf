# Fail2ban filter for terminal.leblais.net (Authelia protected)
# Detects failed authentication attempts (401) in Caddy JSON logs

[Definition]
failregex = ^.*"remote_ip":"<ADDR>".*"host":"terminal\.leblais\.net".*"status":401.*$
ignoreregex =
datepattern = {^LN-BEG}"ts":{EPOCH}
