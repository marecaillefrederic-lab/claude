# Fail2ban filter for adguard.leblais.net
# Detects failed authentication attempts on /control/login endpoint

[Definition]
failregex = ^.*"remote_ip":"<ADDR>".*"host":"adguard\.leblais\.net".*"uri":"/control/login".*"status":(401|403).*$
ignoreregex =
datepattern = {^LN-BEG}"ts":{EPOCH}
