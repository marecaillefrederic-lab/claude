# Fail2ban filter for files.leblais.net (Authelia protected)
# Detects failed authentication attempts (401) in Caddy JSON logs

[Definition]
failregex = ^.*"remote_ip":"<ADDR>".*"host":"files\.leblais\.net".*"status":401.*$
ignoreregex =
datepattern = {^LN-BEG}"ts":{EPOCH}
