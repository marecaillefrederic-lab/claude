# Fail2ban filter for torrent.leblais.net (HTTP Basic Auth)
# Detects failed authentication attempts (401) in Caddy JSON logs

[Definition]
failregex = ^.*"remote_ip":"<ADDR>".*"host":"torrent\.leblais\.net".*"status":401.*$
ignoreregex =
datepattern = {^LN-BEG}"ts":{EPOCH}
